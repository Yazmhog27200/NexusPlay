apiVersion: v1
kind: Service
metadata:
  name: leaderboard-service
  namespace: nexusplay
  labels:
    app: leaderboard-service
spec:
  selector:
    app: leaderboard-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
