apiVersion: v1
kind: Service
metadata:
  name: dns-ha
  namespace: nexusplay
  labels:
    app: dns-ha
spec:
  selector:
    app: dns-ha
  ports:
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
