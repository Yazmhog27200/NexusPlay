apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: nexusplay
  labels:
    app: game-service
spec:
  selector:
    app: game-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
